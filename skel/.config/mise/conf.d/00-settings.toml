[settings]
experimental = true
yes = true
legacy_version_file = true
idiomatic_version_file_enable_tools = [
  "crystal",
  "elixir",
  "go",
  "java",
  "node",
  "python",
  "ruby",
  "terraform",
  "yarn"
]

# this doesn't work with everything, at least right now
# [settings.npm]
# bun = true

[settings.python]
uv_venv_auto = true
