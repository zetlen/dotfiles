" Leader must be set before any <leader> mapping is defined, so it gets its own
" fragment that sorts ahead of everything else.

let mapleader = ","   "<leader> is comma
let g:mapleader = "," "<leader> is comma everywhere
