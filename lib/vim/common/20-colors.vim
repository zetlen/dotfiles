" The actual colorscheme is chosen per-editor (it has to load after that
" editor's plugin manager has put it on the runtimepath). Only the engine-
" agnostic bits live here.

set background=dark                  " because it's how i feel on the inside
syntax enable                        " all syntaces, forever!
